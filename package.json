{
  "name": "@lazurio/github-app-broker",
  "version": "0.8.0",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Lazurio/github-app.git"
  },
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "npm run license:check && node --check src/core.mjs && node --check src/broker.mjs && node --check src/worker.mjs && node --check adapter/broker-client.mjs && node --check adapter/brokered-gh.mjs && node --check scripts/check-licenses.mjs && node --check test/broker.test.mjs && node --check test/brokered-gh.test.mjs && node --check test/worker.test.mjs && npm run worker:dry-run",
    "dev:worker": "wrangler dev",
    "license:check": "node scripts/check-licenses.mjs",
    "test": "node --test",
    "worker:dry-run": "wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
